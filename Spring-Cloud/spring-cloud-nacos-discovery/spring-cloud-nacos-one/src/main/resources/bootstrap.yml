server:
  port: 8080 # 微服务应用端口
  servlet:
    context-path: / # 应用映射
spring:
  application:
    name: nacos-discovery-one # 微服务应用名称
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 # nacos的服务端地址